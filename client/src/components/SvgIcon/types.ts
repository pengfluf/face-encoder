export enum SvgIconId {
  logo = 'logo',
  monkey = 'monkey',
  spinner = 'spinner',
  copy = 'copy',
  checkmark = 'checkmark',
  cross = 'cross',
}

export enum SvgIconSize {
  sm = 'sm',
  md = 'md',
}
